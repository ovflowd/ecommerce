dist: trusty
os:
  - linux
sudo: required
language: groovy
script:
  - cd products-api
  - sudo ./grailsw test-app
  - cd ../purchase-api
  - sudo ./grailsw test-app
  - cd ../
after_success:
  - bash <(curl -s https://codecov.io/bash)
